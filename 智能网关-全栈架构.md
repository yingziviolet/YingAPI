# 项目二：LLM 智能网关 + 控制台（全栈定位）

> 给新会话的说明：这是一份已收敛的架构方案，背景调研已完成，直接按此开工，不要重新发散选型。
> 项目目标：**面试用 + 真实落地**——自己每天真用（自己所有 LLM 调用都走它），7×24 运行。
> 姊妹项目：`E:\后端+agent\01-记账中枢-后端架构.md`（后端定位的记账 agent，是本网关的第一个真实业务流量来源）。

## 一句话定位

自研 LLM 网关基础设施：**数据面**（异步流式转发、语义缓存、难度感知路由、渠道熔断、token 级计量）+ **控制面**（React 控制台：渠道管理与切换、额度监控、实时请求流大盘）。

## 差异化定位（调研结论，面试话术）

- 基础网关（one-api 43k★ / new-api / WaLiAPI / LiteLLM）是红海，它们的**路由全是静态优先级**；语义缓存、难度路由、熔断、异常检测这层**几乎无人做**——这是本项目的核心价值
- 面试可主动对比："市面网关卷的是协议聚合和账号池，我认为价值在智能调度层"
- **性质红线**：只管理**自己合法持有的 API key**。绝不做 sub2api 式"订阅转 API/拼车共享"（违反厂商 ToS 的灰产方向，简历毒药）。所有额度数据产自自己网关的计量，不逆向厂商任何非公开接口
- 数据面/控制面分离本身就是面试架构考点

## 技术栈（已定，勿改）

| 层 | 选型 | 理由 |
|---|---|---|
| 数据面 | Python 3.12 + FastAPI + asyncio + httpx | SSE 流式透传、连接池、背压——Python 异步硬核考点 |
| 缓存/存储 | PostgreSQL + pgvector（语义缓存）+ Redis（限流窗口/熔断状态） | |
| 控制面后端 | FastAPI 管理 API（与数据面同库不同进程） | 渠道 CRUD、统计查询、规则配置 |
| 控制台前端 | Vite + React + TypeScript + Tailwind + shadcn/ui + ECharts | SPA 够用，勿上 Next.js |
| 实时推送 | WebSocket（FastAPI 原生）：请求流 live-tail、告警 | 前后端各一个考点 |
| 基础指标 | Prometheus + Grafana | 自研控制台管业务操作，Grafana 管基础设施指标——"该自研自研、该用现成用现成"是加分取舍 |
| 部署 | Docker Compose + GitHub Actions CI | |

## 总体架构

```
клиенты(记账agent / Claude Code / 任意 OpenAI 兼容客户端)
      │  OpenAI 兼容协议: POST /v1/chat/completions (stream)
      ▼
┌─数据面(FastAPI 异步)──────────────────────────────┐
│ 1 鉴权层:平台发放的虚拟 key → 租户/配额校验          │
│ 2 语义缓存:embedding 相似度 ≥ 阈值 → 直接返回        │
│    (精确匹配缓存先行,二期升级语义)                    │
│ 3 智能路由:                                        │
│    ├ 难度感知:短/简单请求 → 降级便宜模型             │
│    ├ 渠道选择:权重+成本+实时健康度                   │
│    └ 手动 pin:控制台可强制指定渠道                   │
│ 4 熔断器:渠道错误率/延迟滑动窗口(Redis) →            │
│    OPEN → 半开探测 → 恢复;失败自动 failover 下一渠道 │
│ 5 转发:httpx 流式透传(SSE chunk 逐段转发)            │
│ 6 计量:usage 解析 → token/成本记账(异步落库,不阻塞)  │
│ 7 审计:请求日志(脱敏) + trace_id 全链路              │
└──────┬───────────────────────────────────────────┘
       ▼
  上游渠道注册表(自己的 key):OpenAI / DeepSeek / 通义 /
  Kimi / Ollama 本地 …(密钥加密存储)

┌─控制面───────────────────────────────────────────┐
│ 管理 API(FastAPI):渠道 CRUD、路由规则、统计聚合、    │
│ 预算配置、虚拟 key 管理                             │
│ React 控制台:                                      │
│ ├ 渠道面板:健康状态灯、一键启停/切换、优先级拖拽      │
│ ├ 额度大盘:各渠道 token/成本曲线、预算进度条、        │
│ │   "按当前速率 N 天后耗尽"预测                      │
│ ├ 实时请求流:WebSocket live-tail(模型/延迟/命中)     │
│ ├ 智能层成绩单:缓存命中率、降级省钱比例、熔断记录      │
│ └ 告警中心:预算 80%、key 异常消耗、渠道熔断 →        │
│     同时推 Telegram/微信                            │
└──────────────────────────────────────────────────┘

┌─哨兵(Celery beat / 后台任务)──────────────────────┐
│ 用量异常检测:消耗突增/疑似 key 泄漏模式 → 告警        │
│ 渠道主动健康探测、日成本报告推送                      │
└──────────────────────────────────────────────────┘
```

## 核心设计点（面试话术来源）

1. **SSE 流式透传**：上游 chunk 到达即转发，首 token 延迟不劣化；连接池复用、上游超时与客户端断连的双向取消——asyncio 并发控制的展示位
2. **语义缓存**：请求规范化 → embedding → pgvector 余弦相似度 ≥ 阈值命中；带 TTL 与模型维度隔离；**命中率与省钱金额是简历上最漂亮的数字**（有记账 agent 的真实流量验证，不是压测编的）
3. **难度感知路由**：启发式（长度/是否含代码/工具调用）+ 小模型打分，简单请求降级便宜模型；配质量回评（抽样用强模型复评降级答案）证明"降级不降质"
4. **熔断器**：滑动窗口错误率 → OPEN（快速失败+failover）→ 半开放量探测 → 恢复；状态机全程可在控制台观察——比背《熔断器模式》八股强一万倍
5. **计费一致性**：流式响应的 usage 统计、异步落库的最终一致、与上游账单的日对账
6. **虚拟 key 体系**：给每个客户端（记账 agent、自己的 IDE）发独立 key，配独立预算/限流——key 泄漏时异常检测哨兵能定位到具体 key

## 功能路线图

- **P1（第 1 周）**：透明代理跑通——OpenAI 兼容入口、渠道注册表、流式透传、token 计量落库；把自己日常 LLM 调用切过来（真实流量 day one）
- **P2（第 2–3 周）**：精确缓存 → 语义缓存；熔断 + failover；滑动窗口限流；Prometheus 指标
- **P3（第 3–5 周）**：React 控制台（渠道面板 → 额度大盘 → WebSocket live-tail → 告警中心）
- **P4**：难度感知路由 + 质量回评；用量异常哨兵；日成本报告；姊妹项目正式切入
- 时间配比纪律：**后端 60% / 前端 25% / 打磨 15%**，前端能演示就收手

## 第一周逐日任务

- D1：脚手架（FastAPI + SQLAlchemy + Alembic + Docker Compose）；渠道注册表模型 + 密钥加密存储
- D2：/v1/chat/completions 非流式转发跑通（单渠道）；虚拟 key 鉴权
- D3：SSE 流式透传（chunk 逐段转发、断连取消）；trace_id 贯穿
- D4：usage 解析与 token 计量异步落库；多渠道 + 静态优先级路由
- D5：精确匹配缓存（规范化请求 hash）；命中率统计
- D6：管理 API：渠道 CRUD、用量统计聚合接口
- D7：把自己的日常调用（IDE/脚本）切到网关；观察真实日志修 bug；README + 架构图

## 与项目一的关系

记账中枢（`E:\后端+agent\01-记账中枢-后端架构.md`）三期把 LLM 调用切到本网关（只改 base_url），成为第一个真实业务流量源。面试互相引用："语义缓存在姊妹项目真实流量上命中率 X%"。
